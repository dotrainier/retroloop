# RetroLoop Socket.io Server

Separate Express + Socket.io process, run independently from the Next.js app.
Handles real-time only: connections, rooms, presence, and cursor broadcasting.

## Run locally

npm install
npm run dev
